



codex resume 019fff25-c357-7fc1-834f-b3ce38a5cdb7





configurable 的使用比RuntimeContext  更加通用



### 静态图 什么时候更加适合动态图,是不是动态图更加的通用





runtime-web 是我调试使用的,可以绕过权限,本来就不会发布到线上使用



我们本次迁移,不需要兼容旧的服务和使用方式



我如何看到一次完整发送给agent的提示词和内容?是给model的真实内容,不是langgraph这一层的,应该如何看到,debug?还是如何



这个后面要集成到前端



```
_browser_subagent 这些子智能体都是工具,那么他是会被_general_purpose_subagent 调用到还是会被主agent调用到?
```



deepagent 会自带 一个任务的agent,那么应该如何修改这一层呢?

简单的来说



核心顺序是：

  1. 先学生命周期钩子：before_agent、before_model、awrap_model_call、awrap_tool_call、
     after_agent。

  2. 再学 Run 准备：PrepareAgentRunMiddleware 的 sandbox、prompt、checkpoint 与恢复幂等。
  3. 然后学工具治理：参数清洗、错误转换、task 重试、PR/plan mode 权限限制。
  4. 最后学模型可靠性：队列消息、wrap-up、fallback、provider 消息清洗、单调用 timeout。

  配套新增了流程图：docs/open-swe-learning/architecture/premium/21-middleware-learning-
  roadmap.drawio。











异常场景的一些考虑



比如是否可以从某个断点继续



模型的并发和队列如何实现的

